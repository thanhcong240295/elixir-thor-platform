#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Running repo pre-commit checks..."

cd "$ROOT_DIR"
bash scripts/review-staged.sh
bash scripts/dev-all.sh precommit